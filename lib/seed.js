// Starts empty — subjects, notes, and deadlines are now created for real
// by signed-in users through the app (see server.js: POST /api/subjects,
// POST /api/notes/:code, POST /api/deadlines) instead of being seeded
// with placeholder classes and sample notes.
export const seedSubjects = [];

export const seedNotes = {};

export const seedDeadlines = [];

