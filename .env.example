# Copy this file to .env and fill in real values. Never commit .env.

# From Google Cloud Console → APIs & Services → Credentials
# (see README.md for the step-by-step setup)
GOOGLE_CLIENT_ID=350486193346-dis8bc5fqefsncpv00grsd1i5n6f66lt.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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

# From console.anthropic.com → API Keys. Powers Nora, the in-app AI study
# buddy. Without this, Nora replies with a setup message instead of chatting.
ANTHROPIC_API_KEY=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

# Any long random string — used to sign session cookies.
# Generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=8a32ae8441a8f61cce6a658089400c6c314c21056d025a946285ebedaad9254c573adc59a2075c140bed434c02920edd

# Where this backend itself is reachable (must match the redirect URI
# you register in Google Cloud, with /auth/google/callback appended).
BACKEND_URL=http://localhost:4000

# Where the frontend lives — the browser is sent here after sign-in.
# If you're serving the frontend from this same server (the default),
# leave this equal to BACKEND_URL.
FRONTEND_URL=http://localhost:4000

# From https://console.anthropic.com/settings/keys — powers the "Nora"
# AI study buddy chat. Without this, the chat will reply with an error.
ANTHROPIC_API_KEY=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

PORT=4000
NODE_ENV=development
